{
  "name": "engineering-mcp-server",
  "version": "1.0.0",
  "description": "Engineering MCP Server with artificial project and team data for large-scale engineering organizations",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc",
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "docker:build": "docker build -t engineering-mcp-server .",
    "docker:run": "docker run -it engineering-mcp-server"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}