{
  "name": "hrm-mcp-server",
  "version": "1.0.0",
  "description": "HRM MCP Server with artificial employee data",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc",
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "docker:build": "docker build -t hrm-mcp-server .",
    "docker:run": "docker run -it hrm-mcp-server"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}